// is a string?
  // const assert = {
  //   isString: function(assertionToCheck) {
  //     if (assertionToCheck !== String) {
  //       throw new Error("Assertation failed: " + assertionToCheck + " is not a string");
  //     }
  //   };
  // };


// is a note object?



// is equal?
